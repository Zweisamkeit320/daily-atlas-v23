# 今日万象 v2.5.0 LTS 维护记录

候选建立日期：2026-09-02  
候选状态：`LOCAL_CANDIDATE`  
基线：Git 标签 `v2.4.4` / 提交 `b3523c4`  
工作分支：`codex/v2.5.0`

## 稳定基线

- 用户确认 v2.4.4 在夸克同源 A→B 中版本、Service Worker、收藏、偏好、轻量离线、飞行模式五项、德语女声和图文一致全部通过。
- 用户确认夸克、vivo、微信内置浏览器在 Wi-Fi 与移动数据六格通过。
- 用户提交 v2.4.4 第 3 天稳定观察：页面打不开、数据丢失、反复更新、图文串项、页面裁切、德语持续失败、离线重开失败及其他阻断问题均为“无”，结论 `PASS`。
- 上述真机与时间证据来自用户实际操作；没有提供的浏览器版本、逐日时间戳或截图不被自动补造。

## v2.5.0 候选状态

| 项目 | 状态 | 说明 |
|---|---|---|
| 功能范围与用户数据兼容 | `PASS` | 五池规模、存储键、备份 schema 与 v2.4.4 A→B 合同保持不变 |
| 医学当前内容筛查 | `PASS` | 500/500 自动筛查通过，独立内容复核绑定当前 SHA-256 并通过；不宣称医生签字 |
| 完整自动化 | `PASS` | `npm test` exit 0；313/313 单元测试及三引擎、双基址、弱网、离线、更新、持久化、故障注入与 WCAG 均通过 |
| 完整审计 ZIP / 静态 ZIP | `PENDING` | 必须精确封包并干净解压复验 |
| 独立六维审查 | `PENDING` | 审查者不直接修改文件 |
| v2.4.4 → v2.5.0 同源 A→B | `PENDING` | 部署后由实体手机验证 |
| Android Chrome | `BLOCKED` | 当前设备未安装 Chrome |
| iPhone Safari | `BLOCKED` | 当前没有 iPhone |

## 最终版维护边界

v2.5.0 发布后进入 `maintenance-only`。只有可复现核心故障、安全／兼容性问题、来源许可纠正、危险内容纠正或可访问性阻断才允许制作 v2.5.x；不再增加功能或内容规模。完整规则见 `docs/LTS_POLICY_v2.5.0.md`。
