# 今日万象 v2.6.0 实体设备验收矩阵

当前状态：`PASS — 安卓主路径生产验收完成，进入长期冻结维护期`。

记录日期：2026-09-03（Asia/Shanghai）。本结论来自用户在真实安卓手机上的最终确认，不是桌面自动化、模拟 User-Agent 或维护者远程遥测。用户确认 v2.6.0 LTS Visual Edition 安卓主路径生产验收通过；没有提供的浏览器版本、截图和逐格计时不作补造。

| 浏览器 | Wi-Fi | 移动数据 |
|---|---|---|
| 夸克 | PASS（用户实机确认） | PASS（用户实机确认） |
| vivo 系统浏览器 | PASS（用户实机确认） | PASS（用户实机确认） |
| 微信内置浏览器 | PASS（用户实机确认） | PASS（用户实机确认） |

本轮沿用的验收口径：首页与诊断页可开、版本为 v2.6.0、Service Worker 已接管；图书和电影各换 5 次，原书封／海报成功时与文字同步，失败时显示同一条目的主题插画且不残留上一项图片；可滚动到德语和医学；女声可听见；原收藏、偏好、轻量离线在完全关闭后保留；飞行模式重开仍能看到五项。用户的最终结论覆盖上述安卓主路径口径。

Android Chrome：`BLOCKED — 当前设备未安装 Chrome`。

iPhone Safari：`BLOCKED — 当前没有 iPhone`。

`BLOCKED` 仅表示缺少对应设备证据，不是产品故障，也不影响已经完成的安卓主路径验收。

不得为了测试先清除站点数据。异常时先保存浏览器版本、网络类型、完整 URL、诊断摘要和截图。
